using UnityEngine;

public class PedestalWeaponHolder : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
      //spawnear 1 arma en cada pedestal de la escena  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
